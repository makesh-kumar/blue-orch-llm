// Development environment — must match the PORT in web-backend/.env
// When using `ng serve`, HTTP calls go to this URL for the running backend.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3500',
};
